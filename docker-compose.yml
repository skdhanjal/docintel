services:
  db:
    image: pgvector/pgvector:pg16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: docintel
      POSTGRES_USER: docintel
      POSTGRES_PASSWORD: docintel_dev
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: